

package main
import(
    "fmt"
)
func main() {
    var t int
    var n,m,s int
    fmt.Scan(&t)
    
    for i:=0; i<t; i++ {
        fmt.Scan(&n,&m,&s)
        if (m+s-1)%n ==0 {
            fmt.Println(n)
        }else {
            fmt.Println((m+s-1)%n)
        }
    }
}
